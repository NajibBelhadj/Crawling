<form method="post" action="{{ path('provider_hotel_img_delete', {'id': provider_hotel_img.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provider_hotel_img.id) }}">
    <button class="njo">Delete</button>
</form>
